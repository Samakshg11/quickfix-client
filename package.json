{
  "name": "quickfix-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "vite": "^5.0.11",
    "@vitejs/plugin-react": "^4.2.1",
    "tailwindcss": "^3.4.1",
    "autoprefixer": "^10.4.17",
    "concurrently": "^8.2.2"
  }
}
